// core
import React from 'react';

// styles
import styles from './Footer.module.scss';

export const Footer = () => {
    return (
        <footer className={styles.footer}>
            Footer
        </footer>
    );
};